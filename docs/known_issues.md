FCL+Musa 已知问题
=================

1. 调试环境（任务 1.5）仍未配置 WinDbg KD，需在部署前准备 KDNET 或 USB KD。
2. 当前驱动构建为 Debug 配置，未执行 WHQL 签名；发布需使用 Release + 签名脚本。
3. Driver Verifier 仅在自测中检测其启用状态，未自动开启。建议在测试环境显式启用 `/standard` 规则。
4. 自测覆盖目前集中于 API 层；尚未写入单独的内核单元测试工程（任务 8.1）。
5. 文档为初稿，后续如需国际化或更细的 API 细节，可基于 docs/*.md 扩展。
